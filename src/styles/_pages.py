"""Page-specific styles: hero parallax, blog masonry, CV-specific bits.

Reserved as the home for page-scoped CSS that can't be generalized into a
component yet. Epic B will add `.masonry-columns` here. Today this file is
intentionally a thin placeholder so future per-page CSS has an obvious
landing spot without bloating `_components.py`.
"""

PAGES_CSS = """
/* Page-specific styles land here.
   Epic B will introduce `.masonry-columns` for the blog grid;
   hero parallax, CV section, and blog detail extras will follow. */
"""
